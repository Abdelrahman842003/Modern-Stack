APP_NAME=Laravel
APP_ENV=testing
APP_KEY=base64:V5GZJ4/VLV6BuuS9Dm52YrPRwAHMGO3wxxptH5VnKEE=
APP_DEBUG=false
APP_URL=http://localhost

LOG_CHANNEL=stdout
LOG_LEVEL=error

# Database for testing (SQLite in-memory)
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Redis for testing (use simple hostname)
CACHE_STORE=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Sanctum
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1

# Webhook Configuration
WEBHOOK_SECRET=test-webhook-secret-key
NODE_NOTIFY_URL=http://node-notify:3001/notify

# Mail
MAIL_MAILER=array

# Disable unnecessary services in tests
BROADCAST_CONNECTION=null
FILESYSTEM_DISK=local
